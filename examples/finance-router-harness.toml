name = "intent-router-harness"
version = "0.1.0"
description = "面向金融意图路由实验的独立配置。"
agent_paths = ["../agent.md"]
skill_roots = ["../skills"]
max_skill_body_chars = 6000
max_reference_body_chars = 6000
max_reference_count = 4

[[bindings]]
skill = "finance-routing"
load = "metadata"

[[bindings]]
skill = "bill-payment-routing"
load = "metadata"
